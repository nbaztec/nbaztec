[build]
publish = "nbaztec/public"
command = "hugo --baseURL $URL"

[build.environment]
HUGO_VERSION = "0.79.1"
HUGO_THEME = "repo"

[context.deploy-preview]
command = "hugo --buildFuture --buildDrafts --baseURL $DEPLOY_PRIME_URL"
